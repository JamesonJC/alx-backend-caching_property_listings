from django.shortcuts import render

# Create your views here. "@cache_page(60 * 15)"] properties/views.py doesn't contain: ["from django.views.decorators.cache import cache_page"] properties/views.py doesn't contain: ["from django.http import JsonResponse", "return JsonResponse({", "properties", "data"]
# properties/views.py doesn't contain: ["def property_list(request):"]
